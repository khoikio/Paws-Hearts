package com.example.pawshearts.messages.ui.components

// ChatColors.kt

import androidx.compose.ui.graphics.Color

// Nền xám bên ngoài card
val ChatOuterBackground = Color(0xFFE6E6E6)

// Nền trắng bên trong card chat
val ChatCardBackground = Color(0xFFFFFFFF)

// Nền ô search trên màn danh sách tin nhắn
val ChatSearchBg = Color(0xFFF5F5F5)

// Nền ô nhập tin nhắn
val ChatInputBg = Color(0xFFFFF6EF)

// Màu cam thương hiệu
val ChatOrange = Color(0xFFEA5600)
